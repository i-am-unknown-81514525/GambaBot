[project]
name = "gamba-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "server",
    "bot_dc",
]
